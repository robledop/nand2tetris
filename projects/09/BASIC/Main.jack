class Main
{
    function void main()
    {
        var Shell shell;
        do Display.init();
        let shell = Shell.new();

        do shell.run();

        do shell.dispose();
        return;
    }
}