class Util 
{
    function char lowerCaseChar(char c) 
    {
        var char c;
        if ((c > 64) & (c < 91)) 
        {
            return c + 32;
        } 
        else 
        {
            return c;
        }
    }    
}